using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static class DamageCalculator {

	public static float CalculateDamage(string moveName) {
        //get the combo that was used
        //calculate damage based on what keys it required
        float damage = 0f;
        return damage;
    }
}
